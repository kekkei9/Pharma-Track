import React from 'react'
import { useNavigate } from 'react-router-dom'
import './RolePage.scss'

const RolePage = (props) => {
  const navigate = useNavigate()

  return <div className="RolePage">RolePage</div>
}

export default RolePage
