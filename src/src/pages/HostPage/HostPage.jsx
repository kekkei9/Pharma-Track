import React from 'react'
import { useNavigate } from 'react-router-dom'
import './HostPage.scss'

const HostPage = (props) => {
  const navigate = useNavigate()

  return <div className="HostPage">HostPage</div>
}

export default HostPage
